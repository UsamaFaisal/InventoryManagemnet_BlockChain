import React from 'react';

const footerStyle = {
  backgroundColor: '#333',
  color: '#fff',
  padding: '20px',
  textAlign: 'center'
};

const Footer = () => {
  return (
    <footer style={footerStyle}>
      {/* Add footer content here */}
    </footer>
  );
};

export default Footer;
